<div class="container">
  <div class="texts-wrapper">
    <div class="contents-title text-center">
      <div class="row">
        <% if @item_searches.present? %>
          <h2 class="d-inline-block bg-light my-3 ml-4">ジャンル検索結果(全<%= @item_searches.count %>件)</h2>
          <div class="col-9">
            <div class="row">
              <% @item_searches.each do |item| %>
                <div class="col-md-3 my-4">
                  <div class="card w-100 bg-transparent border-0" style="width: 18rem;">
                    <%= link_to item_path(item.id) do %>
                      <%= image_tag item.get_item_image(300,200) %>
                    <% end %>
                    <div class="card-body">
                      <%= item.name %></br>
                      <% if item.is_active == true %><%# 商品が販売している場合 %>
                        ¥<%= item.add_tax_price.to_s(:delimited) %>
                      <% else %><%# 商品が販売を停止している場合%>
                        <p>販売停止中</p>
                      <% end %>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
        <% end %>
      </div>
    </div>
    <div class="d-flex justify-content-center mt-3 custom-pagination">
      <%= paginate @item_searches_all %>
    </div>
  </div>
</div>
</div>